# Examples

Generated examples are intentionally not committed in 0.1.0. Reproduce them with:

```shell
java -jar backsmith-cli/target/backsmith.jar create sample-layered --architecture layered --yes
java -jar backsmith-cli/target/backsmith.jar create sample-hexagonal --architecture hexagonal --yes
```
